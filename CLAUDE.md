# takenbyninjas.com — Blog + TomBot

Static blog published from Google Docs, with a RAG-based AI chatbot (TomBot). Hosted on Netlify with auto-deploy on push to main.

## Tech stack

- **Blog**: Python publish pipeline → static HTML/CSS. Vanilla JS. CRT/teletext theme.
- **TomBot**: Netlify Function (Node.js), Claude Sonnet, Voyage AI embeddings, Nile pgvector DB.
- **Hosting**: Netlify (every push = 1 build = costs money).

## Commands

```bash
# Preview locally
npx http-server -p 8000              # or: python3 -m http.server 8000

# Publish a post (interactive — opens preview, waits for approval)
python3 publish.py "Post Title"

# Regenerate homepage / archive
python3 generate_homepage.py
python3 generate_archive.py

# TomBot: update content pipeline
python3 scripts/parse_content.py      # markdown → JSON
python3 scripts/load_content_v3.py    # JSON → embeddings → DB

# TomBot: run locally
unset ANTHROPIC_API_KEY && netlify dev --port 8888
```

## Architecture

```
publish.py                  # Google Docs → HTML (the big one)
generate_homepage.py        # Builds index.html from posts-metadata.json
generate_archive.py         # Builds words/index.html
pylib/templates.py          # HTML templates for all generators
pylib/config.py             # Shared config
posts-metadata.json         # Source of truth for all posts
lib/styles/styles.css       # All CSS (CRT/teletext theme)
lib/img/                    # Images (compressed via sips)

projects/tombot/index.html  # TomBot frontend (NOT generated — edit directly)
netlify/functions/chat.js   # TomBot backend (RAG + Claude)
netlify/functions/system-prompt.md  # TomBot behaviour prompt (no facts)
docs/tombot/tombot-content-v3.md    # TomBot content source of truth
```

## Generated files — do NOT edit

These are generated by scripts. Edit the generator, not the output:
- `index.html` ← `generate_homepage.py`
- `words/index.html` ← `generate_archive.py`
- `words/{slug}/index.html` ← `publish.py`
- `about/index.html` ← `publish.py`

Exception: `projects/tombot/index.html` is hand-edited, not generated.

Before committing regenerated files, verify generators match output:
```bash
python3 generate_archive.py && git diff words/index.html
python3 generate_homepage.py && git diff index.html
```
If diff shows unexpected changes, the generator is out of sync — fix the generator, not the HTML.

## Publishing workflow

`publish.py` is interactive by default — it generates a preview, opens it in the browser, and waits for Tom to approve before writing files.

1. Run `python3 publish.py "Post Title"` (interactive mode)
2. Script generates preview and opens in browser
3. Tom reviews, edits Google Doc if needed
4. Tom types "yes" in terminal when satisfied
5. Script publishes, regenerates homepage/archive, commits

Only use `echo "yes" | python3 publish.py "Post Title"` when Tom explicitly asks to skip preview.

## Google Docs markup

Frontmatter fields (at top of Google Doc): `url`, `date`, `meta-desc`, `tags`, `no-crt`, `hero-image`.

Inline markers:
- `[HOZ]` → `<hr />`
- `[CAPTION]` → `<figcaption>`
- `[ENDSNIP]` → excerpt end marker
- `[QUOTE]` → `<blockquote>`

Soft returns (`\u000b`) in Google Docs become `\n` in processing — this matters for line-break handling in publish.py.

## Ways of working

### Role
You are a pragmatic web dev partner. Bias toward shipping. Keep responses short. Show don't tell. Prove it works.

### Delegation
- **Tom** owns: design decisions, visual/design choices, prioritisation, commit/deploy approval.
- **Claude** owns: implementation, testing, debugging, verification.
- **Visual verification**: Claude checks via Preview MCP (screenshot, inspect, console). Never ask Tom to check manually — show the evidence.
- **Design choices**: Ask Tom rather than guessing. When it's a visual/aesthetic decision, present options — don't pick for him.

### Workflow
```
understand → plan → build → test locally → show work → WAIT FOR APPROVAL → commit → WAIT FOR APPROVAL → push
```

### Rules
- **Never push without explicit permission.** Every push = Netlify build = costs money.
- **Never commit without asking first.** Show what will be committed.
- **Never claim "should work" without proof.** Use preview tools or test output.
- **Verify with preview tools** — don't ask the user to check manually.
- **Batch changes** — iterate on localhost, push once when done.
- **Bias toward shipping.** Keep responses short and direct.

### Divergent vs convergent thinking
- When exploring ideas, sparring, or making design decisions: **just talk.** No survey forms (AskUserQuestion). One question at a time, conversationally.
- When there's a genuine binary/ternary choice after discussion: forms are fine.
- When presenting options: 2–3 with tradeoffs, then recommend one.

### Engineering principles
- Read before write. Understand existing code before modifying.
- Don't over-engineer. Ship the simplest thing that works.
- Don't add features beyond what was asked.
- Clean semantic HTML, including file/directory naming.
- Test desktop + mobile on localhost before claiming done.
- Number all lists in proposals and multi-point responses (makes feedback easier).

### Debugging
- **3-strike rule.** If 3 consecutive attempts don't produce a clear answer, stop. Write up what you know and ask whether to continue or change approach. Don't grind.
- Inspect, don't guess. Read source files, grep for property names, check actual state.

## Environment variables

- `ANTHROPIC_API_KEY` — Claude API
- `VOYAGE_API_KEY` — Voyage AI embeddings
- `NILEDB_URL` — Nile PostgreSQL (pgvector)

## Known quirks

- `publish.py` runs twice when piped input (`<<< "yes"`) — EOF on second prompt is expected.
- `sips` crop flag: use `--cropToHeightWidth`, not `-c`.
- Image format detection uses magic bytes (PNGs were being saved as .jpg).
- CRT CSS selectors need both `p > img` and `p > a > img` for linked images.
- Caption paragraphs can contain inline images — `caption_handled` flag prevents skipping.

## TomBot — architecture details

TomBot is a RAG chatbot at `/projects/tombot/` that answers questions about Tom's career. It's intentionally a retrieval bot, not a creative assistant — it only answers questions it has training data for.

### Content pipeline (source of truth → DB)
```
docs/tombot/tombot-content-v3.md          # Source of truth — edit THIS file
  → python3 scripts/parse_content.py      # markdown → JSON
  → scripts/parsed_content.json           # intermediate (don't edit)
  → python3 scripts/load_content_v3.py    # JSON → Voyage embeddings → pgvector DB
```

### RAG flow (user query → answer)
1. User query → Voyage AI embedding
2. Match against pre-embedded question variations (threshold: 0.70)
3. If no match → bot deflects gracefully (never hallucinate)
4. TOP_K=1 (single match to prevent cross-section bleed)
5. Retrieved content → Claude Sonnet → streamed response

### Key design decisions
- **System prompt is behavioural only** — no facts about Tom. All factual content comes from RAG.
- **Follow-up suggestions** — only suggest topics from `<follow_up_suggestions>` block in retrieved content. Never invent topics.
- **DB wipe on reload** — `load_content_v3.py` deletes ALL chunks before inserting (no stale data).
- **Dates in retrieved content are always correct.** If they conflict with Claude's training data, retrieved content wins.

### Content format (tombot-content-v3.md)
```markdown
## SECTION.ID

**Questions that route here:**
- Question variation 1
- Question variation 2

**Suggested follow-ups:**
- "follow-up question" [TARGET.SECTION]

Answer content here.
```

## Docs

- `docs/architecture.md` — full technical architecture
- `docs/blog-prd.md` — product requirements
- `docs/tombot/tombot-content-v3.md` — TomBot content (source of truth)
